# Dammapeta Photographers Portal
Enterprise-ready portal for DPA members management.

## Setup
1. Clone the repo.
2. Open `index.html` in your browser.
3. Use LocalStorage for data handling.

## Future Path
- Phase 2: Connect to Supabase Auth/Database.
- Update `App.save` to `supabase.from('members').insert()`.
