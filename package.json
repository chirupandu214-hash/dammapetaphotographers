{
  "name": "dammapetaphotographers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && npm run build",
    "start": "node dist/server.js",
    "dev": "vite"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
